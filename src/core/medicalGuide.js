// src/core/medicalGuide.js

// העתק את כל הקוד ממודול מדריך החובש שיצרנו קודם
const MedicalGuide = {
    // המלצות לפי סוג תלונה, מבוסס על המדריך
    recommendations: {
      // כל ההמלצות שהגדרנו
    },
    
    getRecommendations: function(complaint) {
      // הפונקציה שיצרנו
    },
    
    checkRedFlags: function(complaint, answers) {
      // הפונקציה שיצרנו
    }
  };
  
  // פונקציית עזר לבדיקת תשובות
  function answerContains(answers, keyword, value) {
    // הפונקציה שיצרנו
  }
  
  module.exports = MedicalGuide;